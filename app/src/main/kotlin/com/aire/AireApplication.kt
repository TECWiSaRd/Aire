package com.aire

import android.app.Application

/**
 * Application entry point. Kept intentionally thin in this step — dependency
 * wiring (Room, repository, services) is assembled here in a later step.
 */
class AireApplication : Application()
